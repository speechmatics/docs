# DO NOT EDIT — generated by scripts/generate-agent-stt-spec.ts.
#
# Source: agent-transcription-websocket-v2-input.json
#         agent-transcription-websocket-v2-output.json
#         (openapi-specifications repo, auto-generated/)
#
# Message summaries, servers and the binary AddAudio message come from
# spec/agent-stt.overlay.yaml. Edit that file, then run `npm run build:agent-stt-spec`.

asyncapi: 3.0.0
id: urn:com:speechmatics:agent-stt-api
defaultContentType: application/json
info:
  title: Speechmatics Agent STT
  version: 2.0.0
  contact:
    name: Speechmatics Support
    url: https://www.speechmatics.com/product/support/
    email: support@speechmatics.com
  externalDocs:
    description: Agent STT Reference
    url: https://docs.speechmatics.com/api-ref/agent-stt-websocket
servers:
  default:
    host: eu.rt.speechmatics.com/v2/agent
    protocol: wss
    protocolVersion: v13 (RFC 6455)
    description: Agent STT server
channels:
  publish:
    address: /
    messages:
      StartRecognition:
        $ref: "#/components/messages/StartRecognition"
      AddAudio:
        $ref: "#/components/messages/AddAudio"
      EndOfStream:
        $ref: "#/components/messages/EndOfStream"
      SetRecognitionConfig:
        $ref: "#/components/messages/SetRecognitionConfig"
      GetSpeakers:
        $ref: "#/components/messages/GetSpeakers"
      ForceEndOfUtterance:
        $ref: "#/components/messages/ForceEndOfUtterance"
  subscribe:
    address: /
    messages:
      RecognitionStarted:
        $ref: "#/components/messages/RecognitionStarted"
      AudioAdded:
        $ref: "#/components/messages/AudioAdded"
      AddPartialSegment:
        $ref: "#/components/messages/AddPartialSegment"
      AddSegment:
        $ref: "#/components/messages/AddSegment"
      SpeechStarted:
        $ref: "#/components/messages/SpeechStarted"
      SpeechEnded:
        $ref: "#/components/messages/SpeechEnded"
      StartOfTurn:
        $ref: "#/components/messages/StartOfTurn"
      EndOfTurn:
        $ref: "#/components/messages/EndOfTurn"
      EndOfTranscript:
        $ref: "#/components/messages/EndOfTranscript"
      Error:
        $ref: "#/components/messages/Error"
      Warning:
        $ref: "#/components/messages/Warning"
      Info:
        $ref: "#/components/messages/Info"
      SpeakersResult:
        $ref: "#/components/messages/SpeakersResult"
operations:
  publish:
    action: send
    channel:
      $ref: "#/channels/publish"
    messages:
      - $ref: "#/channels/publish/messages/StartRecognition"
      - $ref: "#/channels/publish/messages/AddAudio"
      - $ref: "#/channels/publish/messages/EndOfStream"
      - $ref: "#/channels/publish/messages/SetRecognitionConfig"
      - $ref: "#/channels/publish/messages/GetSpeakers"
      - $ref: "#/channels/publish/messages/ForceEndOfUtterance"
  subscribe:
    action: receive
    channel:
      $ref: "#/channels/subscribe"
    messages:
      - $ref: "#/channels/subscribe/messages/RecognitionStarted"
      - $ref: "#/channels/subscribe/messages/AudioAdded"
      - $ref: "#/channels/subscribe/messages/AddPartialSegment"
      - $ref: "#/channels/subscribe/messages/AddSegment"
      - $ref: "#/channels/subscribe/messages/SpeechStarted"
      - $ref: "#/channels/subscribe/messages/SpeechEnded"
      - $ref: "#/channels/subscribe/messages/StartOfTurn"
      - $ref: "#/channels/subscribe/messages/EndOfTurn"
      - $ref: "#/channels/subscribe/messages/EndOfTranscript"
      - $ref: "#/channels/subscribe/messages/Error"
      - $ref: "#/channels/subscribe/messages/Warning"
      - $ref: "#/channels/subscribe/messages/Info"
      - $ref: "#/channels/subscribe/messages/SpeakersResult"
components:
  messages:
    StartRecognition:
      summary: Initiates a new recognition session.
      payload:
        $ref: "#/components/schemas/StartRecognition"
    AddAudio:
      summary: A binary chunk of audio. The server confirms receipt by sending an AudioAdded message.
      contentType: application/octet-stream
      payload:
        $ref: "#/components/schemas/AddAudio"
    EndOfStream:
      summary: Declares that the client has no more audio to send.
      payload:
        $ref: "#/components/schemas/EndOfStream"
    SetRecognitionConfig:
      summary: |
        Allows the client to re-configure the recognition session.

        :::note

        The `language` field can be included in SetRecognitionConfig, but its value must match the language specified in the initial StartRecognition message.
        Attempting to change the language or any other transcription configuration parameter not listed below will result in an error.

        :::
      payload:
        $ref: "#/components/schemas/SetRecognitionConfig"
    GetSpeakers:
      summary: Requests any detected speaker identifiers to be returned.
      payload:
        $ref: "#/components/schemas/GetSpeakers"
    ForceEndOfUtterance:
      summary: "Signals the end of a speaker turn. Intended for `turn_config.turn_detection_mode: external`, where the client detects turn ends itself; with the default `vad` mode turn ends are detected automatically."
      payload:
        $ref: "#/components/schemas/ForceEndOfUtterance"
    RecognitionStarted:
      summary: Server response to StartRecognition, acknowledging that a recognition session has started.
      payload:
        $ref: "#/components/schemas/RecognitionStarted"
    AudioAdded:
      summary: |
        Server response to AddAudio, indicating that audio has been added successfully.

        :::info

        When clients send audio faster than real-time, the server may read data slower than it's sent. If binary `AddAudio` messages exceed the server's internal buffer, the server will process other WebSocket messages until buffer space is available. Clients receive `AudioAdded` responses only after binary data is read. This can fill TCP buffers, potentially causing WebSocket write failures and connection closure [with prejudice](https://websockets.spec.whatwg.org#the-closeevent-interface). Clients can monitor the WebSocket's [`bufferedAmount`](https://www.w3.org/TR/websockets#dom-websocket-bufferedamount) attribute to prevent this.

        :::
      payload:
        $ref: "#/components/schemas/AudioAdded"
    AddPartialSegment:
      summary: An interim, best-guess preview of the segment currently being built. Subject to change.
      payload:
        $ref: "#/components/schemas/AddPartialSegment"
    AddSegment:
      summary: A finalized, stable segment, sent when a segment boundary is reached. It will not change.
      payload:
        $ref: "#/components/schemas/AddSegment"
    SpeechStarted:
      summary: Sent when voice activity detection detects the onset of speech.
      payload:
        $ref: "#/components/schemas/SpeechStarted"
    SpeechEnded:
      summary: Sent when voice activity detection detects the end of speech.
      payload:
        $ref: "#/components/schemas/SpeechEnded"
    StartOfTurn:
      summary: Sent when turn detection marks the start of a speaker turn.
      payload:
        $ref: "#/components/schemas/StartOfTurn"
    EndOfTurn:
      summary: Sent when turn detection marks the end of a speaker turn. It is the last message of a turn.
      payload:
        $ref: "#/components/schemas/EndOfTurn"
    EndOfTranscript:
      summary: Server response to EndOfStream, sent after the server has finished sending all segment messages.
      payload:
        $ref: "#/components/schemas/EndOfTranscript"
    Error:
      summary: Error messages sent from the server to the client. After any error, transcription is terminated and the connection is closed.
      payload:
        $ref: "#/components/schemas/Error"
    Warning:
      summary: Warning messages sent from the server to the client.
      payload:
        $ref: "#/components/schemas/Warning"
    Info:
      summary: Additional information sent from the server to the client.
      payload:
        $ref: "#/components/schemas/Info"
    SpeakersResult:
      summary: Server response to GetSpeakers, returning any detected speaker identifiers.
      payload:
        $ref: "#/components/schemas/SpeakersResult"
  schemas:
    AddAudio:
      type: string
      format: binary
    AddChannelAudio:
      additionalProperties: false
      properties:
        channel:
          description: The channel identifier to which the audio belongs.
          type: string
        data:
          description: The audio data in base64 format.
          type: string
        message:
          additionalProperties: false
          const: AddChannelAudio
      required:
        - message
        - channel
        - data
      type: object
    AdditionalWord:
      oneOf:
        - $ref: "#/components/schemas/Word"
        - additionalProperties: false
          properties:
            content:
              $ref: "#/components/schemas/Word"
            sounds_like:
              $ref: "#/components/schemas/PronunciationList"
          required:
            - content
          type: object
    AddPartialSegment:
      additionalProperties: false
      description: An interim, best-guess preview of the segment currently being built. Subject to change.
      properties:
        message:
          const: AddPartialSegment
        metadata:
          $ref: "#/components/schemas/SegmentMetadata"
        segment:
          $ref: "#/components/schemas/Segment"
      required:
        - message
        - metadata
        - segment
      type: object
    AddSegment:
      additionalProperties: false
      description: A finalized, stable segment, sent when a segment boundary is reached. It will not change.
      properties:
        message:
          const: AddSegment
        metadata:
          $ref: "#/components/schemas/SegmentMetadata"
        segment:
          $ref: "#/components/schemas/Segment"
      required:
        - message
        - metadata
        - segment
      type: object
    AudioAdded:
      additionalProperties: false
      properties:
        message:
          const: AudioAdded
        seq_no:
          minimum: 0
          type: integer
      required:
        - message
        - seq_no
      type: object
    AudioFormat:
      $ref: "#/components/schemas/AudioFormatRaw"
      additionalProperties: false
      required:
        - type
      type: object
    AudioFormatFile:
      additionalProperties: false
      properties:
        type:
          additionalProperties: false
          const: file
      required:
        - type
    AudioFormatRaw:
      additionalProperties: false
      properties:
        encoding:
          additionalProperties: false
          enum:
            - pcm_f32le
            - pcm_s16le
        sample_rate:
          additionalProperties: false
          const: 16000
        type:
          additionalProperties: false
          const: raw
      required:
        - type
        - encoding
        - sample_rate
    EndOfChannel:
      additionalProperties: false
      properties:
        channel:
          type: string
        last_seq_no:
          type: integer
        message:
          additionalProperties: false
          const: EndOfChannel
      required:
        - message
        - channel
        - last_seq_no
      type: object
    EndOfStream:
      additionalProperties: false
      properties:
        last_seq_no:
          type: integer
        message:
          additionalProperties: false
          const: EndOfStream
      required:
        - message
        - last_seq_no
      type: object
    EndOfTranscript:
      additionalProperties: false
      properties:
        message:
          const: EndOfTranscript
      required:
        - message
      type: object
    EndOfTurn:
      additionalProperties: false
      description: Sent when turn detection marks the end of a speaker turn. It is the last message of a turn.
      properties:
        message:
          const: EndOfTurn
        metadata:
          $ref: "#/components/schemas/EndOfTurnMetadata"
      required:
        - message
        - metadata
      type: object
    EndOfTurnMetadata:
      additionalProperties: false
      properties:
        end_time:
          description: Time the turn ended, in seconds from session start.
          format: float
          type: number
      required:
        - end_time
      type: object
    Error:
      additionalProperties: false
      properties:
        message:
          const: Error
        reason:
          minlength: 1
          type: string
        type:
          enum:
            - invalid_message
            - invalid_model
            - invalid_language
            - invalid_config
            - invalid_audio_type
            - not_authorised
            - not_allowed
            - job_error
            - protocol_error
            - start_recognition_timeout
      required:
        - message
        - reason
        - type
      type: object
    ForceEndOfUtterance:
      additionalProperties: false
      description: "Signals the end of a speaker turn. Intended for `turn_config.turn_detection_mode: external`, where the client detects turn ends itself; with the default `vad` mode turn ends are detected automatically."
      properties:
        message:
          additionalProperties: false
          const: ForceEndOfUtterance
        timestamp:
          description: Timestamp of the audio data that corresponds to the force end of utterance request. It's the number of seconds since the beginning of the audio.
          format: float
          minimum: 0
          type: number
      required:
        - message
        - timestamp
      type: object
    GetSpeakers:
      additionalProperties: false
      properties:
        final:
          type: boolean
        message:
          additionalProperties: false
          const: GetSpeakers
      required:
        - message
      type: object
    Info:
      additionalProperties: false
      else:
        not:
          required:
            - quality
        required:
          - message
          - reason
          - type
      if:
        properties:
          type:
            const: recognition_quality
      properties:
        message:
          const: Info
        quality:
          enum:
            - broadcast
        reason:
          minlength: 1
          type: string
        type:
          enum:
            - recognition_quality
            - concurrent_session_usage
      then:
        required:
          - message
          - quality
          - reason
          - type
      type: object
    LanguagePackInfo:
      additionalProperties: false
      description: Properties of the language pack.
      properties:
        adapted:
          description: Whether or not language model adaptation has been applied to the language pack.
          type: boolean
        itn:
          description: Whether or not ITN (inverse text normalization) is available for the language pack.
          type: boolean
        language_description:
          description: Full descriptive name of the language, e.g. 'Japanese'.
          type: string
        word_delimiter:
          description: The character to use to separate words.
          type: string
        writing_direction:
          description: The direction that words in the language should be written and read in.
          enum:
            - left-to-right
            - right-to-left
          type: string
      required:
        - word_delimiter
      type: object
    MidSessionTranscriptionConfig:
      additionalProperties: false
      properties:
        enable_partials:
          default: false
          description: Whether or not to send partials as well as finals.
          type: boolean
        language:
          description: Language model to process the audio input, normally specified as an ISO language code
          minLength: 1
          type: string
    Pronunciation:
      minLength: 1
      type: string
    PronunciationList:
      items:
        $ref: "#/components/schemas/Pronunciation"
        additionalProperties: false
      minItems: 1
      type: array
    RecognitionStarted:
      additionalProperties: false
      properties:
        id:
          type: string
        language_pack_info:
          $ref: "#/components/schemas/LanguagePackInfo"
        message:
          const: RecognitionStarted
        orchestrator_version:
          type: string
      required:
        - message
        - id
      type: object
    Segment:
      additionalProperties: false
      properties:
        speaker:
          description: Speaker label (e.g. `S1`), present only when diarization attributed one.
          type: string
        transcript:
          description: The rendered segment text.
          type: string
      required:
        - transcript
      type: object
    SegmentMetadata:
      additionalProperties: false
      properties:
        end_time:
          description: Segment end, in seconds from session start.
          format: float
          type: number
        start_time:
          description: Segment start, in seconds from session start.
          format: float
          type: number
      required:
        - start_time
        - end_time
      type: object
    SetRecognitionConfig:
      additionalProperties: false
      properties:
        message:
          additionalProperties: false
          const: SetRecognitionConfig
        transcription_config:
          $ref: "#/components/schemas/MidSessionTranscriptionConfig"
          additionalProperties: false
      required:
        - message
        - transcription_config
      type: object
    SpeakerDiarizationConfigRule:
      $comment: If speaker_diarization_config.speakers or get_speakers is provided, diarization must be speaker.
      if:
        properties:
          speaker_diarization_config:
            anyOf:
              - required:
                  - speakers
              - required:
                  - get_speakers
            type: object
        required:
          - speaker_diarization_config
      then:
        properties:
          diarization:
            enum:
              - speaker
        required:
          - diarization
      x-noAdditionalProperties: true
    SpeakersInputItem:
      additionalProperties: false
      properties:
        label:
          description: Speaker label, which must not match the format used internally (e.g. S1, S2, etc).
          minLength: 1
          not:
            additionalProperties: false
            pattern: ^(S\d+|UU|\s+.*|\S+.*\s+)$
          type: string
        speaker_identifiers:
          items:
            description: Speaker identifiers.
            format: bytes
            type: string
          minItems: 1
          type: array
          uniqueItems: true
      required:
        - label
        - speaker_identifiers
      type: object
    SpeakersResult:
      properties:
        message:
          const: SpeakersResult
        speakers:
          items:
            properties:
              label:
                description: Speaker label.
                minLength: 1
                type: string
              speaker_identifiers:
                items:
                  description: Speaker identifiers.
                  format: bytes
                  type: string
                minItems: 1
                type: array
                uniqueItems: true
            required:
              - label
              - speaker_identifiers
            type: object
          type: array
      required:
        - message
        - speakers
    SpeechEnded:
      additionalProperties: false
      description: Sent when voice activity detection detects the end of speech.
      properties:
        message:
          const: SpeechEnded
        metadata:
          $ref: "#/components/schemas/SpeechEndedMetadata"
      required:
        - message
        - metadata
      type: object
    SpeechEndedMetadata:
      additionalProperties: false
      properties:
        end_time:
          description: Time speech ended, in seconds from session start.
          format: float
          type: number
      required:
        - end_time
      type: object
    SpeechStarted:
      additionalProperties: false
      description: Sent when voice activity detection detects the onset of speech.
      properties:
        message:
          const: SpeechStarted
        metadata:
          $ref: "#/components/schemas/SpeechStartedMetadata"
      required:
        - message
        - metadata
      type: object
    SpeechStartedMetadata:
      additionalProperties: false
      properties:
        start_time:
          description: Time speech began, in seconds from session start.
          format: float
          type: number
      required:
        - start_time
      type: object
    StartOfTurn:
      additionalProperties: false
      description: Sent when turn detection marks the start of a speaker turn.
      properties:
        message:
          const: StartOfTurn
        metadata:
          $ref: "#/components/schemas/StartOfTurnMetadata"
      required:
        - message
        - metadata
      type: object
    StartOfTurnMetadata:
      additionalProperties: false
      properties:
        start_time:
          description: Time the turn began, in seconds from session start.
          format: float
          type: number
      required:
        - start_time
      type: object
    StartRecognition:
      additionalProperties: false
      properties:
        audio_format:
          $ref: "#/components/schemas/AudioFormat"
          additionalProperties: false
        message:
          additionalProperties: false
          const: StartRecognition
        transcription_config:
          $ref: "#/components/schemas/TranscriptionConfig"
          additionalProperties: false
        turn_config:
          $ref: "#/components/schemas/TurnConfig"
          additionalProperties: false
      required:
        - message
        - audio_format
        - transcription_config
      type: object
    TranscriptionConfig:
      additionalProperties: false
      allOf:
        - $ref: "#/components/schemas/SpeakerDiarizationConfigRule"
        - if:
            properties:
              operating_point:
                const: linden-1
            required:
              - model
              - operating_point
          then:
            properties:
              model:
                const: linden-1
      anyOf:
        - required:
            - model
        - required:
            - operating_point
      properties:
        additional_vocab:
          $ref: "#/components/schemas/WordList"
          additionalProperties: false
        diarization:
          description: |-
            Specify whether speaker labels are added to the transcript.
            The default is `none`.
              - **none**: no diarization labels are added.
              - **speaker**: speaker attribution is performed based on acoustic matching.
          enum:
            - none
            - speaker
          type: string
        domain:
          description: Request a specialized model based on 'language' but optimized for a particular field, e.g. 'finance' or 'medical'.
          minLength: 1
          type: string
        emit_sentences:
          default: false
          description: |-
            Whether or not to split multi-sentence segments on sentence boundaries, so that each segment contains a single sentence.
            The default is `false`.
          type: boolean
        enable_partials:
          default: false
          description: Whether or not to send partials as well as finals.
          type: boolean
        language:
          description: Language model to process the audio input, normally specified as an ISO language code
          minLength: 1
          type: string
        model:
          description: Alias for operating_point. If both model and operating_point are provided, they must have the same value.
          enum:
            - linden-1
          type: string
        operating_point:
          description: |-
            Specify an operating point to use. Operating points change the transcription process in a high level way, such as altering the acoustic model.
            The default is 'standard'.
          enum:
            - linden-1
          type: string
        output_locale:
          description: Language locale to be used when generating the transcription output, normally specified as an ISO language code
          type: string
        speaker_diarization_config:
          additionalProperties: false
          properties:
            get_speakers:
              description: If true, speaker identifiers will be returned at the end of transcript.
              type: boolean
            max_speakers:
              description: Specifies the maximum number of speakers allowed in a single audio stream. If not provided, the number of speakers is unrestricted.
              minimum: 2
              type: integer
            prefer_current_speaker:
              description: If true, the diarization engine will prefer to stick with the currently active speaker if good enough, even if not the best. This is useful for cases where we can flip incorrectly between similar speakers during a single speaker section.
              type: boolean
            speaker_sensitivity:
              description: Controls the number of unique speakers detected; a higher value is likely to result in more unique speakers being identified in the output. You may want to increase this value if you are seeing fewer speaker labels than expected. The default is 0.5.
              maximum: 1
              minimum: 0
              type: number
            speakers:
              items:
                $ref: "#/components/schemas/SpeakersInputItem"
                additionalProperties: false
              type: array
          type:
            - object
            - "null"
        transcript_filtering_config:
          additionalProperties: false
          description: Configuration for applying filtering to the transcription
          properties:
            remove_disfluencies:
              description: If true, words that are identified as disfluencies will be removed from the transcript. If false, they are tagged in the transcript as 'disfluency'.
              type: boolean
            replacements:
              items:
                additionalProperties: false
                properties:
                  from:
                    description: The pattern to be replaced.
                    type: string
                  to:
                    description: The word or phrase to replace the 'from' value.
                    type: string
                required:
                  - from
                  - to
                type: object
              type: array
          type:
            - object
            - "null"
      required:
        - language
      type: object
    TurnConfig:
      additionalProperties: false
      properties:
        turn_detection_mode:
          default: vad
          description: |-
            How the end of a speaker turn is detected.
            The default is `vad`.
              - **vad**: turn ends are detected automatically from voice activity.
              - **external**: turn ends are signalled by the client with the `ForceEndOfUtterance` message.
          enum:
            - vad
            - external
          type: string
      type: object
    Warning:
      additionalProperties: false
      properties:
        duration_limit:
          minimum: 0
          type: number
        message:
          const: Warning
        reason:
          minlength: 1
          type: string
        type:
          enum:
            - duration_limit_exceeded
            - idle_timeout
            - session_timeout
            - add_audio_after_eos
            - speaker_id
            - unsupported_language
      required:
        - message
        - reason
      type: object
    Word:
      minLength: 1
      pattern: \S
      type: string
    WordList:
      description: List of custom words or phrases that should be recognized. Alternative pronunciations can be specified to aid recognition.
      items:
        $ref: "#/components/schemas/AdditionalWord"
        additionalProperties: false
      type: array

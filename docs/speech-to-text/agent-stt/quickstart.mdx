---
title: Quickstart
pagination_prev: null
pagination_next: null
description: Transcribe a conversation with Agent STT.
---

import CodeBlock from '@theme/CodeBlock';
import { Grid } from '@radix-ui/themes';
import { LinkCard } from "@site/src/theme/LinkCard";
import { Languages, Braces, Blocks } from 'lucide-react';

import pythonAgentSttExample from "./assets/quickstart.py?raw"

# Quickstart

Agent STT returns transcription a segment at a time and reports the speech and turn events a voice agent needs, so you know when a speaker has finished and the transcript is ready to send to an LLM.

## Using the Agent STT SDK

The Agent STT SDK is a Python client for the Agent STT service. It runs no VAD or turn detection of its own — either the service closes turns, or your application does.

### 1. Create an API key

[Create an API key in the portal](https://portal.speechmatics.com/settings/api-keys), which you'll use to securely access the API. Store the key as a managed secret. See [Authentication](/get-started/authentication) for temporary keys and other options.

:::info
Enterprise customers may need to speak to [Support](https://support.speechmatics.com) to get your API keys.
:::

### 2. Install the SDK

```
pip install speechmatics-agent-stt pyaudio
```

:::note
`pyaudio` is required for microphone input in this quickstart.
:::

### 3. Run the example

Set your API key in the environment, then run the script:

```
export SPEECHMATICS_API_KEY=<your-api-key>
```

<CodeBlock language="python">
  {pythonAgentSttExample}
</CodeBlock>

Speak into your microphone. You should see partials as you talk, then a final segment when the turn closes:

```
[partial] hello I'd like to check on my
[final]   S1: Hello, I'd like to check on my order.
[turn]    end at 3.24s
```

Press `Ctrl+C` to stop.

## Understanding the output

Agent STT works in segments rather than word groups. A segment is closed at a turn or content boundary, and each one arrives as a single message:

- **`AddPartialSegment`** — an interim preview of the segment being built. Each one replaces the previous, so don't concatenate them.
- **`AddSegment`** — the finalized segment. This is the stable output to pass to your LLM.

Read the text from `message["segment"]["transcript"]`. When diarization is enabled, `message["segment"]["speaker"]` carries the speaker label (`S1`, `S2`, and so on).

`StartOfTurn` and `EndOfTurn` bound a turn; `EndOfTurn` is the signal that the speaker has finished and your agent can respond.

:::note
`SpeechStarted` and `SpeechEnded` report voice activity, which is not the same as a turn boundary — a pause mid-sentence produces `SpeechEnded` without ending the turn.
:::

## Choosing who closes the turn

The service needs a boundary to close a segment on. By default its own VAD provides one, and it emits the speech and turn events above.

If your application already does its own endpointing — Pipecat, LiveKit, a push-to-talk button — turn the service's VAD off and close each turn yourself:

```python
from speechmatics.agent_stt import TurnConfig, TurnDetectionMode

client = AgentSttAsyncClient(
    transcription_config=TranscriptionConfig(language="en"),
    turn_config=TurnConfig(turn_detection_mode=TurnDetectionMode.EXTERNAL),
)
```

Then close the turn when your side decides speech has ended:

```python
client.finalize()                        # from a sync callback
await client.force_end_of_utterance()    # from async code
```

Either call is stamped with the audio position at the moment it was made, so the service cuts the turn where you heard the end of speech rather than wherever the message lands. The flushed segment comes back as a normal `AddSegment`.

## Next steps

<Grid columns={{initial: "1", md: "2"}} gap="3">
  <LinkCard
    title="Speech to Text languages"
    description="The languages you can pass as language"
    href="/speech-to-text/languages"
    icon={<Languages/>}
  />
  <LinkCard
    title="Agent STT Reference"
    description="Every WebSocket message, handshake response and error code"
    href="/api-ref/agent-stt-websocket"
    icon={<Braces/>}
  />
  <LinkCard
    title="Integrations"
    description="Run Agent STT inside LiveKit, Pipecat or Vapi"
    href="/integrations-and-sdks"
    icon={<Blocks/>}
  />
</Grid>

---
description: Configure remote access to the appliance.
---

# Remote Access

By default the Appliance can be accessed over SSH with the username `smadmin` and password `changeme`. We recommend disabling password access and setting up access via SSH key.

[Key-based SSH Access (Ubuntu)](https://help.ubuntu.com/community/SSH/OpenSSH/Keys)
